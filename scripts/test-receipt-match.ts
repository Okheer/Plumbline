import assert from 'node:assert/strict';import {encodeFunctionData,parseAbi,zeroHash,type Hex} from 'viem';import {matchesIdentityTransaction as match} from '../packages/backend/src/identity/receipt-match.ts';
const step={from:'0x1111111111111111111111111111111111111111',to:'0x2222222222222222222222222222222222222222',data:'0x12345678' as Hex};
const tx={from:step.from,to:'0xdb9b1e94b5b69df7e401ddbede43491141047db3',value:0n,input:encodeFunctionData({abi:parseAbi(['function redeemDelegations(bytes[] contexts, bytes32[] modes, bytes[] executions)']),functionName:'redeemDelegations',args:[['0x'],[zeroHash],[(step.to+'0'.repeat(64)+step.data.slice(2)) as Hex]]})};
assert(match(tx,step));assert(!match({...tx,from:'0x0000000000000000000000000000000000000000'},step));assert(!match({...tx,value:1n},step));assert(!match(tx,{...step,data:'0x1234'}));assert(!match(tx,{...step,to:'0x0000000000000000000000000000000000000000'}));assert(match({...tx,to:step.to,input:step.data},step));console.log('PASS: direct/wrapped receipt matching; rejects wrong sender, value, target and calldata');
